spring:
  application:
    name: salarylens

  datasource:
    url: jdbc:postgresql://localhost:5432/salarylens_db
    username: postgres
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}

    cache:
      type: redis
      redis:
        time-to-live: 3600000


jwt:
  secret: ${JWT_SECRET:my-super-secret-key-for-dev-at-least-32-chars}
  expiration: 86400000

server:
  port: 8080

openai:
  api:
    key: ${OPENAI_API_KEY}
    url: https://api.openai.com/v1/chat/completions
    model: gpt-3.5-turbo
